# CLAUDE.md — 『Algorithmic』

> Claude Code가 이 레포(`Algo_MasterB`)에서 작업할 때 참조하는 최상위 컨텍스트.
> 이 문서 하나로 작업이 시작 가능해야 한다. 여기 답이 있는 것은 다시 묻지 않는다.
>
> **이 레포는 독립 프로젝트다.** 다른 레포에 지식을 넘기지 않고, 다른 레포를 전제하지도 않는다.
> 알고리즘을 이해하고 구현하는 데 필요한 것은 **전부 이 안에서 완결된다.**

---

## 0. 이 프로젝트가 존재하는 이유

알고리즘 자료는 세 진영으로 갈라져 있고, 셋 다 반쪽이다.

- **코딩테스트 자료:** 문제 풀이 스킬만 다룬다. 그 알고리즘이 실제 시스템 어디에 박혀 있는지는 말하지 않는다. 시험이 끝나면 버려진다.
- **시스템·로보틱스 자료:** A\*, D\* Lite, ICP, 상태 기계를 다루지만 **그 밑바닥이 결국 BFS·다익스트라·DP라는 사실을 연결해주지 않는다.** 라이브러리 호출법만 남는다.
- **인프라·백엔드 자료:** B-Tree, LSM, 블룸 필터, 일관성 해싱을 "이런 게 있다"로 소개하고 끝난다. **그것이 트리·해시·확률의 응용이라는 사실**은 연결되지 않는다.

**이 책은 셋이 같은 계보임을 증명한다.**

```
BFS  →  다익스트라  →  A*  →  D* Lite  →  시공간 A*  →  CBS
 └ 격자 최단거리     └ 가중 그래프   └ 휴리스틱   └ 동적 재계획   └ 다중 에이전트

해시 테이블  →  일관성 해싱  →  블룸 필터  →  HyperLogLog
 └ O(1) 조회      └ 샤딩·리밸런싱   └ 확률적 집합   └ 확률적 카디널리티

이진 탐색 트리  →  균형 트리  →  B-Tree  →  B+Tree  →  LSM Tree
 └ 정렬 유지         └ 최악 보장     └ 블록 I/O   └ DB 인덱스   └ 쓰기 최적화
```

이 세 줄이 설계 사상 전체다. 왼쪽 끝에서 오른쪽 끝까지 **끊지 않고** 간다.

**언어는 도구다.** 모든 개념을 **Python과 C++ 두 버전으로 동시에** 제시한다(§2-2). 언어 기초는 알고리즘을 짜는 데 필요한 범위에서 **이 레포 안에서 완결한다**(§4-3).

**대상 독자:** 일반 프로그래밍 경험이 있고, ① 코딩테스트 대비와 ② 실무 시스템 알고리즘을 하나의 체계로 익히려는 사람. 도달 기준은 **"시니어 개발자가 설명할 수 있어야 하는 수준"**이다.

---

## 1. 확정된 방향 (결정표)

| # | 결정 | 내용 | 근거 |
|---|---|---|---|
| A-01 | 산출물 형식 | 인터랙티브 웹앱. `build.py` + `content/*.md` → `assets/bundle.js` 번들 엔진 | 의존성 제로로 `file://`에서 도는 파이프라인. 알고리즘은 시각화 효과가 가장 큰 분야다 |
| A-02 | 레포 가시성 | public | §4-7 집필 규범이 강제로 적용된다 |
| A-03 | 코드 언어 | **Python + C++ 전 예제 동시 제시.** 탭 전환 코드 블록(§2-2) | 이중 언어가 이 책의 정체성 |
| A-04 | 코드 배치 순서 | 본문은 **Python을 먼저, C++을 뒤에** | Python 쪽이 알고리즘의 뼈대가 더 잘 드러난다. C++은 같은 알고리즘의 저수준 대응으로 읽힌다 |
| A-05 | 목표 난도 | 실버~골드 수준 + 구현·시뮬레이션형 + 기업 채용형(Lv2~3). **플래티넘 이상 경기용 기법은 비목표.** 단 **실무에서 실제로 쓰이는 고급 구조는 난도와 무관하게 다룬다**(Part XV) | §3 |
| A-06 | 1순위 파트 | **Part I 유형 판별.** 자료구조보다 앞 | 알고리즘 학습의 실질 병목은 지식의 부재가 아니라 **문제-알고리즘 매핑의 실패**다. 이 명제가 책 전체의 구성 근거다 |
| A-07 | 최단경로 통합 | 다익스트라와 A\*/D\* Lite를 **하나의 Part(V)로 연속 서술.** 분리 금지 | 같은 계보를 두 번 배우는 것은 순손실 |
| A-08 | 실무 파트 깊이 | **개념 + 동작하는 최소 구현(200줄 내외).** 라이브러리 호출이 아니라 내부를 짠다 | 설명할 수 있는 수준이 기준 |
| A-09 | 실무 파트 우선순위 | ① 설계 패턴(XII) ② 실시간·동시성(XI) ③ 경로계획(V에 통합) ④ 시스템 규모 자료구조(XV) ⑤ 상태추정·인지(XIII) | ⑤는 수학 비중이 높아 후순위 |
| A-10 | 문제 참조 | **유형별 대표문제 번호·제목 + 사고과정.** 정답 코드 전문은 싣지 않는다 | 목적은 사고과정 이식이지 코드 암기가 아니다. 답이 보이면 사고과정이 죽는다 |
| A-11 | 폰 복습 | **단일 HTML 파일**(`build.py --single` → `algobook.html`) + **유형판별 Anki 플래시카드** | 단일 HTML은 위젯이 그대로 살아 있고 외부 의존성이 0이다. EPUB은 위젯이 전부 죽어서 채택하지 않는다 |
| A-12 | 학습 관리 | SRS 복습 큐(SM-2), localStorage | 알고리즘은 안 쓰면 즉시 휘발한다 |
| A-13 | 화면 대응 | **완전 반응형.** 폰에서도 본문·목차·검색·위젯이 동작한다 | GitHub Pages로 폰에서 바로 열린다. 위젯은 폭에 따라 레이아웃을 재배치한다(§8) |
| A-14 | 기준 툴체인 | **Python 3.13 / C++17(g++ 13)** | §1-3 |
| A-15 | 독립성 | 외부 레포·프레임워크 문서에 지식을 위임하지 않는다 | 이 문서 하나, 이 레포 하나로 완결되어야 한다 |

### 1-1. 경로계획 알고리즘 명칭 (정확히 쓸 것)

이 계보는 문헌마다 표기가 흔들리고 존재하지 않는 이름(`P*` 등)이 잘못 통용되기도 한다. 이 책은 다음 명칭만 쓴다.

| 이름 | 정체 | 위치 |
|---|---|---|
| **A\*** | 다익스트라 + 휴리스틱. 목표까지의 추정 비용 `h(n)`을 더해 탐색 순서를 편향 | V-5 |
| **LPA\*** | Lifelong Planning A\*. 증분 탐색의 기반 | V-7 |
| **D\* / D\* Lite** | *Dynamic* A\*. 새 장애물 발견 시 전체 재계산 없이 영향 구간만 갱신 | V-7 |
| **Theta\* / Any-angle** | 격자 방향 제약을 제거한 경로 | V-6 |
| **JPS** | Jump Point Search. 균일 격자의 대칭성을 제거해 A\*를 가속 | V-6 |
| **RRT / RRT\* / PRM** | 샘플링 기반. 격자가 불가능한 고차원 구성 공간 | V-13 |

### 1-2. DP와 최단경로의 관계 (서술 방향 고정)

"DP는 최단경로에 쓰인다"는 표현은 인과가 반대다. **최단경로 문제가 DP의 한 사례다.** 벨만 방정식

```
d(v) = min over u ( d(u) + w(u, v) )
```

이 곧 점화식이고, 다익스트라·벨만포드·플로이드워셜은 **이 점화식을 어떤 순서로 푸느냐**의 차이일 뿐이다. **V-4에서 이 관계를 정면으로 서술한다.** 이 챕터를 통과하면 DP와 그래프가 한 덩어리로 붙는다. 다른 챕터에서 이 관계를 반대로 서술하지 않는다.

### 1-3. 기준 툴체인과 측정 환경 (수치를 쓸 때 반드시 이 환경)

§4-4·§4-7이 요구하는 "실측 수치"는 **다음 환경에서 실제로 실행한 값**만 쓴다. 추정치 금지.

| 항목 | 값 |
|---|---|
| OS | Ubuntu 24.04 LTS (x86-64) |
| Python | **CPython 3.13.12** (`python3.13`) |
| C++ | **g++ 13.3.0**, `-std=c++17 -O2` |
| CPU / RAM | 4 코어 / 15 GB |

본문에 수치를 적을 때는 이 문장을 함께 둔다:

> (Linux x86-64 / CPython 3.13 / g++ 13 `-O2` 실측. 절대값은 기기마다 다르지만 자릿수 차이는 어디서나 같다.)

측정 스크립트는 `tools/bench/`에 남긴다. **재현 불가능한 수치는 본문에 넣지 않는다.**

C++ 표준은 **C++17 고정**이다. C++20 이상이 명백히 나은 지점(예: `std::jthread`)에서만 버전을 명시하고 쓰되, 그 경우 C++17 대안도 함께 보인다.

---

## 2. 아키텍처

### 2-1. 엔진의 기본 원칙

- **의존성 제로.** `index.html` 더블클릭으로 열린다. npm·프레임워크·백엔드·CDN 없음.
- **`content/*.md` → `build.py` → `assets/bundle.js`** 번들링. `file://`로 열면 `fetch()`가 CORS로 막히므로, `<script>`가 읽는 JS 파일에 JSON을 박아 서버 없이 동작시킨다.
- **`content/toc.json`이 책의 뼈대.** Part → Chapter 계층, id 기반 링크. 챕터 파일명은 `<id>.md`이고 `content/` 아래 어디에 있어도 빌드가 찾는다.
- **마크다운 확장 문법:** 코드 블록 `title=` / 강조줄 `{3,5-7}`, `::: note/tip/warn/danger/deep/perf/hist/quiz/answer/lead`, 수식, 형광펜 `==`, `<kbd>`, 내부 링크 `[제목](#/id)`.
- **자산 버전 도장.** `build.py`가 `index.html`의 자산 URL에 내용 해시를 붙인다. 브라우저 캐시 때문에 반드시 필요하다.
- **단일 파일 빌드.** `build.py --single` → `algobook.html`(CSS·JS·본문 전부 인라인). 폰·오프라인용.
- **완전 반응형**(A-13). 폰 브레이크포인트를 만든다. 위젯은 폭에 따라 레이아웃을 재배치한다.

### 2-2. `::: dual` — 이중 언어 코드 블록

모든 구현을 Python과 C++ **한 블록 안에서 탭 전환**한다. 좌우 분할이 아니라 탭인 이유: 코드가 길어지면 좌우 분할은 가로 스크롤 지옥이 되고, 창을 절반 폭으로 줄였을 때 무너진다.

````text
::: dual
```python title="BFS — 최단 거리"
from collections import deque
...
```
```cpp title="BFS — 최단 거리"
#include <queue>
...
```
:::
````

**강제 규칙:** 두 코드는 **동일한 알고리즘·동일한 변수명·동일한 줄 구조**여야 한다. 언어 차이로 불가피하게 갈라지는 지점은 **코드 아래 "언어 차이" 표**로 반드시 명시한다. 두 언어를 동시에 배울 때 가장 위험한 것은 "비슷해 보이는데 다른 것"이다.

대표 예:

| 항목 | Python | C++ |
|---|---|---|
| 힙 기본 방향 | `heapq` = **최소 힙 고정** | `priority_queue` = **최대 힙 기본** |
| 정수 오버플로 | 없음(임의 정밀도) | `int` 오버플로 있음 → `long long` |
| 재귀 깊이 | 기본 1000, `setrecursionlimit` 필요 | 스택 한도까지 |
| 나눗셈 | `//`는 음수에서 내림 | `/`는 0 방향 절삭 |
| 정렬 안정성 | `sort()` 안정 | `std::sort` 불안정, `stable_sort` 안정 |

### 2-3. `::: classify` — 유형 판별 상자

Part I의 결정 트리를 각 알고리즘 챕터에 역으로 심는 장치. **Part II~X의 모든 알고리즘 챕터에 필수.**

```text
::: classify
- 신호어: "최소 이동 횟수", "몇 번 만에", "가장 가까운"
- 제약조건: 간선 가중치가 전부 1 또는 없음, 정점 ≤ 10^5
- 혼동 주의: 가중치가 다르면 → 다익스트라(V-1). 음수면 → 벨만-포드(V-2)
- 반례 함정: 시작점이 여러 개면 다중 시작 BFS(IV-4)
:::
```

`build.py`가 이 블록을 전부 수집해 **Part I 결정트리 부록**과 **Anki 플래시카드 덱**을 자동 생성한다(§2-5). 챕터를 쓰면 유형 판별 훈련 데이터가 자동으로 쌓이는 구조다.

### 2-4. `::: trace` / `::: pitfall` / `::: interview`

| 상자 | 용도 |
|---|---|
| `::: trace` | **손추적(dry run) 훈련.** 코드를 보여주기 **전에** 상태 표를 주고 "이 입력으로 3스텝까지 손으로 채워라". 정답은 `::: answer`로 접는다. 구현력의 근본은 머릿속 실행 모델이다 |
| `::: pitfall` | 실제로 틀리는 지점. 오프바이원, 방문 체크 시점(큐에 넣을 때 vs 뺄 때), 재귀 깊이, 정수 오버플로, 음수 나눗셈, 부동소수 비교 |
| `::: interview` | "이 개념은 기술 면접에서 이렇게 질문된다 + 모범 답변 뼈대". 남발 금지, 실제로 질문되는 지점에만 |

`::: trace`는 §4-5의 필수 목록 챕터에서 **생략 불가**다.

### 2-5. 플래시카드 내보내기

`tools/export_flashcards.py` → **Anki 임포트용 CSV**. Anki는 무료 오픈소스이고 AnkiDroid/AnkiMobile로 폰에서 열린다. 별도 앱 개발 비용 0.

| 덱 | 앞면 | 뒷면 | 소스 |
|---|---|---|---|
| `01_classify` | 지문 요약 + 제약조건 | 알고리즘 이름 + 근거 한 줄 | `::: classify` |
| `02_complexity` | 자료구조/연산 이름 | 시간·공간 복잡도 + 상수 특성 | 각 챕터 복잡도 표 |
| `03_patterns` | 패턴/용어 이름 | 정의 + 도메인 실사례 | `glossary.json` + Part XII |

마일스톤 종료 시 실행하고 `dist/anki/`에 커밋한다.

### 2-6. SRS 복습 큐 (신규 구현)

SM-2 알고리즘, `localStorage`. 챕터 단위가 아니라 **`::: classify` / `::: quiz` 항목 단위**로 스케줄링한다 — 알고리즘 학습에서 휘발하는 것은 "챕터를 읽었다"가 아니라 "이 지문을 보고 유형을 맞출 수 있다"이기 때문이다.

- 저장 키 네임스페이스: `algobook:*`
- 항목 상태: `{ id, ease, interval, due, reps, lapses }`
- 평가 4단계: 모름 / 어려움 / 보통 / 쉬움
- 앱 홈에 "오늘 복습할 N개" 진입점.

---

## 3. 명시적 비목표

| 금지 | 이유 |
|---|---|
| React/Vue/npm 빌드체인 | 의존성 제로 원칙 위반. 위젯은 vanilla JS로 충분하다 |
| 앱 내부 코드 실행 샌드박스 | 코드 블록은 표시·복사 전용. 실제 실행은 IDE와 채점기에서 한다 |
| **경기용 플래티넘 이상 기법**(고급 플로우 심화, 스플레이/링크컷 트리, FFT, 커널 트릭 DP, 접미사 자동자) | 목표 난도 밖. 스코프 크립. **단 실무에서 실제로 쓰이는 고급 구조(B-Tree, LSM, 스킵 리스트 등)는 Part XV에서 다룬다** — 판정 기준은 난도가 아니라 "실제로 쓰이는가"다 |
| 언어 자체의 완전한 문법서 | 이 책은 알고리즘 책이다. 언어는 §4-3 범위에서 완결한다 |
| **정답 코드 전문 수록** | A-10 |
| 검증 안 된 외부 링크 | §4-8 |
| 프레임워크 사용법 튜토리얼(ROS 2, Nav2, RocksDB API 등) | 사용법은 공식 문서의 영역. 단 **"이 프레임워크가 이 패턴/알고리즘을 쓴다"와 그 내부 원리 해부는 이 책의 핵심**이다 |
| 저자 1인칭 서사 | §4-7 |
| EPUB 산출물 | A-11. 위젯이 죽는다 |

---

## 4. 집필 규범

`docs/STYLE.md`가 완전한 규범이다. 아래는 그중 반드시 지켜지는 골자다.

**기본:** 평서형 반말 단정문("~한다", "~이다"). 독자 호칭은 "당신". 헤지 금지, 다만 모르면 모른다고 쓴다. 한 문장에 한 가지. **정의부터 시작하지 말고 문제부터 보인다.** 절당 본문 6,000~12,000자(코드 제외). 분량 채우려고 늘리지 않는다.

### 4-1. 위젯 우선 원칙

**시간에 따라 상태가 변하는 것은 예외 없이 위젯으로 만든다.** 탐색 순서, 테이블 채우기, 포인터 이동, 트리 회전, 힙 sift, 재귀 호출 트리, B-Tree 분할, LSM 컴팩션 — 전부 해당한다. 아스키 다이어그램은 **정적 구조**(그래프 모양, 트리 형태) 표기에만 쓴다. "글로 설명 가능하다"는 이 책에서 근거가 되지 않는다.

### 4-2. 3단 서술 구조 (모든 알고리즘 챕터 강제)

```
① 문제   — 이 알고리즘이 없으면 무엇이 안 되는가. 구체적 상황부터
② 아이디어 — 위젯 + 산문. 코드 없이 원리만. 여기서 이해가 끝나야 한다
③ 구현   — ::: trace → ::: dual → 복잡도 → ::: pitfall → ::: classify
```

**②와 ③ 사이에 `::: trace`가 없으면 그 챕터는 미완성이다.** 코드를 먼저 보여주고 설명하는 순서는 금지한다 — 그 순서는 "읽으면 이해되는데 못 짜는" 상태를 재생산한다.

### 4-3. 언어 기초는 "필요한 만큼, 이 레포 안에서 완결"

내장 함수·표준 라이브러리가 본문에 **처음 등장하면 그 자리에서 정의한다.** "나중에 설명", "검색해봐라", "다른 문서 참조" 전부 금지. 고정 형식:

```text
📖 `heapq.heappush(heap, item)` — 최소 힙에 원소 삽입. O(log n).
   Python의 heapq는 최소 힙 전용이다. 최대 힙이 필요하면 값에 -1을 곱해 넣는다.
   쓰는 곳: 다익스트라(V-1), 우선순위 스케줄러(XI-5).
   C++ 대응: std::priority_queue — 이쪽은 최대 힙이 기본. 반대다.
```

**본문에서의 깊이 상한:** 시그니처 + 한 줄 의미 + 복잡도 + "어디서 쓰이는가" + 반대 언어 대응. **그 이상 파고들 것은 Part 0의 해당 챕터로 링크한다.**

**Part 0은 자립적인 완결 레퍼런스다.** 알고리즘을 짜는 데 쓰이는 Python 표준 라이브러리와 C++ STL은 Part 0에서 **내부 구현·실제 비용·함정까지** 끝낸다. 예: `sort()`의 안정성이 언제 문제가 되는지는 본문에서(실전에서 틀린다), Timsort의 런 병합과 갤로핑은 Part 0-4에서.

Part 0은 **처음부터 끝까지 읽는 곳이 아니라 필요할 때 돌아오는 곳**이다. 본문에서 처음 만났을 때 배우는 것이 기본 경로다.

### 4-4. 복잡도 표기 강제

모든 구현 코드 아래에 **시간·공간 복잡도 + 그 근거 한 줄**을 붙인다. `O(E log V)`만 쓰고 끝내지 않는다 — "각 간선마다 힙에 최대 한 번 push되므로 힙 연산 O(E)회, 각 O(log V)" 식으로 **어디서 그 항이 나왔는지**를 쓴다. 유도할 수 없는 복잡도 표기는 자산이 아니다.

**상수도 말한다.** `O(1)`이라고만 하지 말고 그 상수가 언제 문제가 되는지 쓴다. 실측이 필요하면 §1-3 환경에서 잰다.

각 챕터의 `::: classify` 상자는 Part 0의 **N 범위 → 허용 복잡도 역산 표**를 참조한다.

### 4-5. `::: trace` 필수 지점

다음 챕터는 손추적 블록 없이 넘어갈 수 없다: BFS/DFS, 백트래킹, 다익스트라, 이분 탐색(경계 조건), **모든 DP 챕터**, 유니온 파인드(경로 압축), 세그먼트 트리(구간 분해), KMP(실패 함수), 투 포인터, B-Tree(분할), LSM(컴팩션), 일관성 해싱(노드 추가 시 재배치).

**형식:** 표의 첫 2~3행만 채워 보여주고 나머지는 빈칸 → 독자가 채움 → `::: answer`로 전체 공개.

### 4-6. 실무 연결은 필수, 억지는 금지

**모든 알고리즘 챕터는 최소 한 번, "이 자료구조/알고리즘이 실제 시스템 어디에 박혀 있는가"를 짚는다.** 억지 연결은 금지 — 없으면 쓰지 않고, 마일스톤 보고에 "이 챕터는 실무 연결이 약하다"고 한 줄 남긴다.

**좋은 예:** 유니온 파인드 → 이미지 연결요소 라벨링, 맵 병합 / 트라이 → 명령어 자동완성, IP 라우팅 테이블 / 위상 정렬 → 빌드 시스템·작업 의존성 스케줄링 / 우선순위 큐 → 실시간 태스크 스케줄러, 이벤트 시뮬레이션 / 볼록 껍질 → 로봇 footprint 생성 / 스킵 리스트 → Redis Sorted Set / 머클 트리 → Git 오브젝트 그래프.
**나쁜 예:** "세그먼트 트리는 센서 데이터 구간 조회에 쓸 수도 있다" ← 쓰지 않는다.

### 4-7. 서술 시점과 톤 (예외 없는 강제 규칙)

이 레포는 public이며 누구나 모든 문서·챕터·주석·커밋 메시지를 읽는다는 전제로 쓴다.

**절대 넣지 않는 것:**

- **저자 1인칭 서사 일체.** "나는 이걸 몰랐다", "처음엔 어려웠다", "계속 틀렸다" 류의 학습 경험 고백. **이 책은 독자를 위한 레퍼런스이지 저자의 학습 노트가 아니다.**
- 저자의 이력·학력·경력·나이·직무·과거 사용 기술·구직 활동에 대한 언급.
- 특정 회사명, 채용 시장 서술, 특정 시험·전형에 대한 개인적 평가.
- 자기 능력에 대한 방어적·변명적 서술.
- **AI 어트리뷰션.** 커밋 메시지·코드 주석·본문 어디에도 생성 도구를 언급하지 않는다.

**대신 쓰는 프레이밍:** 모든 서술은 **일반 명제 또는 콘텐츠 설계 근거**로 쓴다. 예를 들어 Part I 도입부는 *"알고리즘 학습의 실질 병목은 지식의 부재가 아니라 문제-알고리즘 매핑의 실패다"* 처럼 쓴다 — 이것은 고백이 아니라 편집 방침의 근거다. 대상 독자층을 서술하는 것(§0) 역시 허용된다.

**적용 범위:** 이 문서 자신을 포함해 레포에 들어가는 모든 파일. README, 챕터 본문, 코드 주석, 커밋 메시지, PR 본문, 부속 문서 전부. 애매하면 §11-3에 따라 확인받는다.

### 4-8. 외부 링크와 문제 참조

- **링크 검증 절대 규칙:** 외부 링크는 **작성 시점에 실제로 접속해서 (1) 살아있는지 (2) 제목이 아니라 실제 내용이 주제와 일치하는지** 확인한 뒤에만 넣는다. 확인 못 한 링크, 추측으로 만든 URL은 **넣지 않는다.** 개별 영상보다 **채널/재생목록 단위 링크**를 우선한다. 확인이 안 되면 비워 둔다. **없는 것이 틀린 것보다 낫다.** 추가할 때마다 같은 커밋에 `docs/LINK_LOG.md`에 한 줄(확인 날짜, URL, 확인 방법)을 남긴다.
- **논문 인용:** Part V·XV의 원전 논문은 **저자·연도·제목**만 쓴다(예: Koenig & Likhachev, 2002, *D\* Lite*). URL은 위 규칙을 통과했을 때만.
- **문제 참조:** 번호 + 제목 + 난도만 표기. URL은 `https://www.acmicpc.net/problem/{번호}` 규칙 URL만 허용(결정론적 패턴이라 검증 가능). 그 외 플랫폼은 **제목만** 쓴다.
- **정답 코드 전문 금지.** `::: quiz`로 **"어떤 신호로 이 유형임을 알아채는가 → 상태를 무엇으로 정의하는가 → 왜 이 자료구조인가"** 3단 사고과정만 서술한다. 코드는 독자가 짠다.
- **문제 선정 기준:** 유형의 원형(canonical)에 가까울 것. 변형·꼼수 문제는 배제. 챕터당 3~5개.

### 4-9. 아스키 다이어그램

한글은 고정폭 글꼴에서 **두 칸**을 차지한다. 선(`│ ─ ┌ └`)보다 **왼쪽에** 한글이 있으면 그림이 깨진다. 선보다 오른쪽의 꼬리 주석은 괜찮다. `tools/check_diagrams.py`가 검사한다.

### 4-10. 챕터 파일 템플릿

새 챕터는 이 골격에서 시작한다.

````markdown
# {번호} {제목}

::: lead
(이 챕터가 답하는 질문 한 문장)
:::

## 1. 문제
(이게 없으면 무엇이 안 되는가 — 구체적 상황부터)

## 2. 아이디어
::: widget {type} {json}
:::
(위젯을 말로 옮긴 산문. 코드 없이 여기서 이해가 끝나야 한다)

## 3. 손으로 따라가기
::: trace
(입력 + 상태 표 앞 2~3행. 나머지는 빈칸)
:::
::: answer
(전체 표)
:::

## 4. 구현
::: dual
```python title="..."
```
```cpp title="..."
```
:::

**복잡도:** 시간 O(...) — (근거 한 줄) / 공간 O(...) — (근거 한 줄)

| 언어 차이 | Python | C++ |
|---|---|---|

::: pitfall
:::

## 5. 어디에 쓰이는가
(§4-6 실무 연결. 억지면 생략하고 보고)

## 6. 이 유형을 알아보는 법
::: classify
- 신호어:
- 제약조건:
- 혼동 주의:
- 반례 함정:
:::

::: interview
(해당되는 경우만)
:::

## 연습
::: quiz
(대표문제 3~5개 — 번호·제목·난도 + 3단 사고과정. 정답 코드 없음)
:::

## 요약
- 

**다음 절**: [{번호} {제목}](#/{id}) — 한 줄 예고.
````

---

## 5. 위젯 라이브러리

의존성 없는 Canvas/SVG + vanilla JS. `assets/widgets/<name>.js`. 정해진 예시에 맞춰 손으로 설계한 애니메이션이며, **스텝 재생/되감기를 기본 제공한다**(자동 애니메이션이 아니라 스텝 기반 — 독자가 한 스텝씩 곱씹어야 하기 때문).

| 위젯 | 무엇을 보여주는가 | 우선순위 | 쓰이는 곳 |
|---|---|---|---|
| `grid-search` | **동일 격자에서 BFS/DFS/다익스트라/A\*를 동시 재생.** 4분할, 방문 순서를 색 농도로. 벽·가중치·휴리스틱을 드래그로 편집하면 즉시 재탐색. **간판 위젯** | ★★★ | IV, V |
| `dp-table` | 점화식에 따라 셀이 채워지는 순서 + 참조 셀로 향하는 화살표 + 역추적 경로. 배낭/LCS/LIS/구간DP 파라미터화 | ★★★ | VIII |
| `heap-ops` | 삽입·삭제 시 sift-up/sift-down. 배열 표현과 트리 표현 동시 표시 | ★★★ | II-8, V-1 |
| `hash-table` | 해시 함수 → 버킷 배치 → 충돌 → 체이닝/개방주소법 → 리해싱 | ★★★ | II-6 |
| `union-find` | 유니온 시 트리 병합, 경로 압축 전후 비교, 랭크 | ★★ | IX-1 |
| `binary-search-bounds` | `while` 조건·중앙값 계산에 따라 경계가 어떻게 달라지는지. **오프바이원의 시각화** | ★★ | VII |
| `sorting-race` | 정렬 알고리즘 동시 경주 + 비교/교환 횟수 카운터 | ★★ | II-7 |
| `tree-traversal` | 전위/중위/후위/레벨 순회 + 재귀 호출 스택 동시 표시 | ★★ | II-10 |
| `backtracking-tree` | 재귀 호출 트리가 뻗고 가지치기로 잘려나가는 것 | ★★ | III-4 |
| `segment-tree` | 구간 쿼리가 O(log n)개 노드로 분해되는 과정, lazy propagation | ★★ | IX-2 |
| `dstar-replan` | **A\*로 경로를 뽑은 뒤 장애물 추가 → 전체 재계산 vs D\* Lite 부분 갱신의 연산량을 카운터로 비교** | ★★ | V-7 |
| `multi-agv-conflict` | 다수 에이전트가 교차로에서 충돌·교착하는 장면 → 예약 테이블/CBS가 푸는 과정 | ★★ | V-8~V-10 |
| `ringbuffer-concurrency` | 생산자·소비자 스레드가 링버퍼를 동시에 밀고 당기는 타임라인. 오버런/언더런 순간 | ★★ | XI-1 |
| `state-machine` | FSM / 계층적 FSM / Behavior Tree의 상태 전이 | ★★ | XI-6, XII-8 |
| `btree-ops` | **B-Tree/B+Tree의 삽입 → 노드 분할 → 높이 증가. 디스크 블록 읽기 횟수 카운터** | ★★ | XV-2 |
| `lsm-compaction` | **memtable → SSTable 플러시 → 레벨 컴팩션. 쓰기 증폭/읽기 증폭을 수치로** | ★★ | XV-3 |
| `bloom-filter` | **비트 배열에 k개 해시가 찍히는 과정, 거짓양성이 발생하는 순간** | ★★ | XV-4 |
| `consistent-hash-ring` | **링 위 가상 노드 배치 → 노드 추가/제거 시 재배치되는 키의 비율** | ★★ | XV-8 |
| `two-pointer` | 두 포인터·슬라이딩 윈도우의 확장·축소 | ★ | VI-3 |
| `complexity-plot` | N 슬라이더에 따라 O(N)/O(N log N)/O(N²)/O(2^N)이 로그 축에서 벌어지는 것 + "1초 안에 되는 선" | ★ | 0-9 |
| `cache-policy` | LRU/LFU/ARC가 같은 접근 패턴에서 서로 다르게 축출하는 것, 히트율 비교 | ★ | XV-9 |
| `kdtree-nn` | KD 트리 분할과 최근접 탐색 시 가지치기 | ★ | XI-9 |
| `skiplist` | 레벨이 확률적으로 쌓이는 과정과 탐색 경로 | ★ | XV-5 |

**★★★ 4종은 M2에서 콘텐츠보다 먼저 만든다.** 나머지는 해당 Part 착수 시 순차 구현. 위젯을 전부 먼저 만들고 콘텐츠를 쓰는 순서는 금지한다.

**위젯 공통 계약:** 모든 위젯은 `window.Widgets['<type>'] = function(el, opts)` 하나를 등록한다. 반응형 대응(§8)은 위젯 자신의 책임이다.

---

## 6. 커리큘럼 — 전체 목차

> 챕터 id는 `iv-3` 형식으로 안정적으로 쓴다. **절 단위 세분화·예제 선택·대표문제 선정은 집필 시점의 저술 판단이다**(§11-1).

### Part 0 — 도구상자

*알고리즘을 쓰기 위한 언어. 자립적인 완결 레퍼런스이며(§4-3), 처음부터 끝까지 읽는 곳이 아니라 필요할 때 돌아오는 곳이다.*

| # | 제목 | 핵심 |
|---|---|---|
| 0-1 | 이 책 읽는 법 | 목적별 경로(코테 급행 / 실무 급행 / 완주), 손으로 짜는 원칙, SRS 사용법 |
| 0-2 | Python 입출력과 기본 타입 | `sys.stdin.readline`이 `input()`보다 빠른 이유, 개행 처리, `map/split`, 다차원 리스트 초기화 함정(`[[0]*n]*m`), 정수의 임의 정밀도 비용 |
| 0-3 | Python 내장 자료구조의 실제 비용 | `list`의 증폭 패턴과 `insert(0)`이 O(n)인 이유, `dict`/`set`의 개방 주소법과 리사이즈, 튜플 vs 리스트, `str` 불변성과 `join` |
| 0-4 | Python 표준 라이브러리 세트 | `collections`(deque/defaultdict/Counter), `heapq`, `bisect`, `itertools`, `functools`(lru_cache/cmp_to_key), `math`, `sys`. **Timsort 내부까지** |
| 0-5 | C++ 입출력과 기본 타입 | `ios_base::sync_with_stdio(false)`, `cin.tie(nullptr)`, `endl` vs `'\n'`, `int` 오버플로와 `long long`, `auto`, 범위 기반 for, UB가 실제로 무엇을 부수는가 |
| 0-6 | C++ 컨테이너 세트 | `vector`(재할당·`reserve`·이터레이터 무효화), `string`, `pair/tuple`, `array`, `queue/deque/stack`, `priority_queue`, `map/unordered_map`, `set/unordered_set`. **각각의 실제 메모리 배치와 캐시 특성** |
| 0-7 | C++ 알고리즘 함수 세트 | `sort`(introsort)·`stable_sort`, `lower_bound/upper_bound`, `next_permutation`, `accumulate`, `max_element`, `fill`, `unique`+`erase`, `nth_element` |
| 0-8 | C++ 반복자·람다·비교자 | 알고리즘을 짜는 데 필요한 만큼의 반복자 카테고리, 람다 캡처, 커스텀 비교자가 만족해야 할 strict weak ordering(어기면 실제로 크래시한다) |
| 0-9 | **N 범위로 알고리즘 역산하기** | 제약조건 → 허용 복잡도 → 알고리즘 후보군 표. "1초 ≈ 10^8 연산"의 실체와 예외 |
| 0-10 | **언어별 상수와 TLE 대응** | 동일 로직 Python vs C++ **실측 벤치마크**(§1-3), PyPy 판단 기준, 재귀→반복 전환, 입출력 병목, 언제 C++로 갈아탈 것인가 |
| 0-11 | **손추적(dry run) 방법론** | 상태 표 그리는 법, 무엇을 관찰할지 고르는 법, 무한 루프·오프바이원을 종이에서 잡는 법 |
| 0-12 | 디버깅과 반례 찾기 | 브루트포스 대조 코드, 랜덤 스트레스 테스트 스크립트(양 언어), 경계값 체크리스트 |
| 0-13 | Python ↔ C++ 대응 치트시트 | 자료구조·함수·관용구 1:1 매핑 (색인용) |

### Part I — 유형 판별 ★ 1순위

*문제를 읽고 5분 안에 "이건 무슨 문제인가"를 결정하는 훈련.*

| # | 제목 |
|---|---|
| I-1 | 왜 판별이 병목인가 — 아는 것과 알아보는 것은 별개의 능력이다 |
| I-2 | 제약조건 역산법 — N, 시간·메모리 제한을 먼저 읽는다 |
| I-3 | 신호어 사전 — "최소 횟수"→BFS, "경우의 수"→DP·조합, "~를 최대화하는 최소값"→매개변수 이분탐색, "구간 합/갱신"→누적합·세그트리, "연결되어 있는지"→유니온파인드·DFS, "순서가 있는 작업"→위상정렬 |
| I-4 | 결정 트리 전문 — 입력 형태(격자/그래프/수열/문자열/기하) × 묻는 것(존재/개수/최적/구성) 2축 매트릭스 |
| I-5 | 오분류 사례집 — 그리디로 보이는 DP, DP로 보이는 BFS, 정렬로 보이는 우선순위 큐, 완전탐색으로 보이는 수학 |
| I-6 | 5분 프로토콜 — 지문 → 제약 역산 → 후보 2개 → 반례 던지기 → 확정 |
| I-7 | 실전 운영 전략 — 시간 배분, 문제 선택 순서, 부분점수 판단, 손절 기준 |

### Part II — 기초 자료구조

| # | 제목 | 실무 연결 |
|---|---|---|
| II-1 | 배열과 동적 배열 | 연속 메모리·캐시 지역성, 증폭 정책 |
| II-2 | 문자열 처리 | 프로토콜 메시지 파싱, 인코딩 |
| II-3 | 연결 리스트 | 개념 이해용. 실전에서 왜 잘 안 쓰는가(캐시 미스) |
| II-4 | 스택 | 재귀의 정체, 괄호·수식, 실행 취소, 콜 스택 |
| II-5 | 큐와 덱 | 메시지 큐, 명령 버퍼 |
| II-6 | 해시(딕셔너리/셋) | 충돌·리해싱, ID→객체 조회 테이블, 해시 DoS |
| II-7 | 정렬 | Timsort/introsort의 정체, 안정성이 언제 문제인가, 커스텀 비교자, 계수·기수 정렬 |
| II-8 | 힙과 우선순위 큐 | 실시간 태스크 스케줄러, 다익스트라의 엔진 |
| II-9 | 이진 탐색 트리와 균형 트리 | `map`/`set`의 내부(레드-블랙 트리), 정렬 상태 유지가 필요한 경우 |
| II-10 | 트리 순회 | 씬 그래프·좌표 프레임 트리, 파일시스템, DOM |

### Part III — 완전탐색·재귀·시뮬레이션

| # | 제목 |
|---|---|
| III-1 | 재귀 사고법 — 기저 조건과 신뢰의 도약 |
| III-2 | 순열·조합·부분집합 생성 (직접 구현 + 라이브러리 양쪽) |
| III-3 | 비트마스크로 집합 다루기 |
| III-4 | 백트래킹과 가지치기 |
| III-5 | **구현·시뮬레이션형** — 델타 배열, 격자 회전, 좌표 변환, 상태 스냅샷 관리 |
| III-6 | 시뮬레이션을 버그 없이 짜는 규율 — 함수 분해, 불변식 명시, 중간 출력 |

### Part IV — 그래프 탐색

| # | 제목 | 실무 연결 |
|---|---|---|
| IV-1 | 그래프 표현 — 인접 행렬/리스트/암묵 그래프 | 맵·의존성·상태공간을 그래프로 보기 |
| IV-2 | DFS | 연결 요소, 사이클 검출 |
| IV-3 | BFS와 최단 거리 | **격자 최단경로의 원형** |
| IV-4 | 0-1 BFS와 다중 시작점 BFS | 다중 소스 동시 확산 |
| IV-5 | 플러드필과 연결 요소 라벨링 | **비전의 blob 검출** |
| IV-6 | 위상 정렬 | **빌드 시스템·작업 의존성 스케줄링** |
| IV-7 | 이분 그래프·사이클 판정 | 자원 충돌 검사 |
| IV-8 | 트리에서의 DFS — 서브트리 크기, 깊이 | |

### Part V — 최단경로 계보 ★ 이 책의 척추

*왼쪽 끝은 코딩테스트, 오른쪽 끝은 다중 로봇 관제 시스템이다. 끊지 않는다.*

| # | 제목 | 구분 |
|---|---|---|
| V-1 | 다익스트라 | 코테 |
| V-2 | 벨만-포드와 음수 간선 | 코테 |
| V-3 | 플로이드-워셜 | 코테 |
| V-4 | **DP와 최단경로는 같은 것이다** — 벨만 방정식, 최적 부분구조, 세 알고리즘이 같은 점화식의 다른 순회 순서임을 보이기 | 이론 |
| V-5 | **A\*** — 휴리스틱의 정의, admissible/consistent, 다익스트라가 `h=0`인 A\*임을 보이기 | 브릿지 |
| V-6 | A\* 변형 — Weighted A\*(최적성을 속도와 교환), JPS, Theta\*/Any-angle | 실무 |
| V-7 | **D\* Lite** — LPA\*에서 출발, 장애물 발견 시 부분 재계획, 전체 재계산 대비 연산량 비교 | 실무 |
| V-8 | **시공간 A\*와 예약 테이블** — 시간 축을 상태에 추가. 다중 에이전트가 서로의 미래 위치를 피하는 방법 | 실무 |
| V-9 | **CBS와 우선순위 기반 계획** — 다중 에이전트 경로 계획(MAPF) | 실무 |
| V-10 | **교착 검출과 회피** — 자원 할당 그래프의 사이클 검출, 은행가 알고리즘, 교차로 상호배제, 라이브락 | 실무 |
| V-11 | **할당 문제** — 헝가리안 알고리즘, auction, 탐욕적 할당의 한계 | 실무 |
| V-12 | **지역 경로계획** — 전역 vs 지역의 역할 분담, DWA·TEB의 원리, 코스트맵 인플레이션 | 실무 |
| V-13 | **샘플링 기반 계획** — RRT, RRT\*, PRM. 격자가 불가능한 고차원 구성 공간 | 실무 |
| V-14 | 계보 총정리 — 한 장의 지도 + 상황별 선택 기준 | 종합 |

### Part VI — 그리디·구간 기법

| # | 제목 |
|---|---|
| VI-1 | 그리디가 성립하는 조건 — 교환 논법 |
| VI-2 | 구간 스케줄링·회의실 배정 (실무: 자원 예약) |
| VI-3 | 투 포인터와 슬라이딩 윈도우 |
| VI-4 | 누적 합과 차분 배열 (실무: 그리드 구간 갱신) |
| VI-5 | 좌표 압축 |
| VI-6 | 스위핑 |

### Part VII — 이분 탐색

| # | 제목 |
|---|---|
| VII-1 | 정렬된 배열에서의 이분 탐색 — 경계 조건 완전 정복 |
| VII-2 | lower_bound / upper_bound / bisect |
| VII-3 | **매개변수 탐색** — "답을 이분 탐색한다"는 발상 |
| VII-4 | 실수 이분 탐색과 종료 조건 |
| VII-5 | 이분 탐색 + 그리디 결합 유형 |

### Part VIII — 동적 계획법

| # | 제목 |
|---|---|
| VIII-1 | DP 사고 절차 — 상태 정의 → 점화식 → 초기값 → 계산 순서 |
| VIII-2 | 메모이제이션 vs 타뷸레이션 |
| VIII-3 | 1차원 DP — 계단, 타일링, 최대 부분합 |
| VIII-4 | 2차원 DP — 격자 경로, 편집 거리 |
| VIII-5 | 배낭 문제 — 0/1, 무한, 부분합 |
| VIII-6 | LIS와 LCS |
| VIII-7 | 구간 DP |
| VIII-8 | 트리 DP |
| VIII-9 | 비트마스크 DP — 외판원 순회(TSP), **다중 지점 순회 최적화** |
| VIII-10 | DP 최적화 입문 — 슬라이딩 윈도우·누적합 결합 |
| VIII-11 | DP 상태 정의 실패 사례집 |

### Part IX — 고급 자료구조·문자열

| # | 제목 | 실무 연결 |
|---|---|---|
| IX-1 | 유니온 파인드 | **연결 요소 라벨링, 맵 병합** |
| IX-2 | 세그먼트 트리와 lazy propagation | 구간 집계 |
| IX-3 | 펜윅 트리(BIT) | |
| IX-4 | 최소 신장 트리 — 크루스칼, 프림 | **로드맵/토폴로지 맵 생성, 네트워크 설계** |
| IX-5 | LCA와 희소 배열 | 좌표 프레임 트리의 공통 조상 |
| IX-6 | 트라이 | **명령어 자동완성, IP 라우팅 테이블** |
| IX-7 | 문자열 매칭 — KMP, 라빈-카프 | 로그 스캔, grep의 정체 |
| IX-8 | 네트워크 플로우 입문 — 최대 유량, 이분 매칭 | **작업 할당(V-11과 연결)** |

### Part X — 수학·기하

| # | 제목 |
|---|---|
| X-1 | 정수론 — GCD/LCM, 유클리드 호제법 |
| X-2 | 소수 — 에라토스테네스의 체, 소인수분해 |
| X-3 | 모듈러 연산과 분할 정복 거듭제곱 |
| X-4 | 조합론 기초 |
| X-5 | 기하 — CCW, 선분 교차, 다각형 내부 판정 (**충돌 검사의 기초**) |
| X-6 | 볼록 껍질 (**footprint 생성**) |
| X-7 | 부동소수점의 함정 — 왜 `==`로 비교하면 안 되는가, EPS 정하는 법, 정수로 우회하기 |

### Part XI — 실무 자료구조와 동시성 ★ 우선순위 상

| # | 제목 | 실사례 |
|---|---|---|
| XI-1 | 링 버퍼(원형 큐) | 센서 스트림, 로그 버퍼, 오버런 정책 |
| XI-2 | 생산자-소비자와 유계 큐 | 제어 루프 ↔ 통신 스레드 분리 |
| XI-3 | 락과 락프리 — 뮤텍스, 조건변수, 원자적 연산, 메모리 순서, SPSC 락프리 큐 | 실시간 경로에서 락이 위험한 이유 |
| XI-4 | 스레드 풀과 작업 큐 | 병렬 이미지 처리, 워크 스틸링 |
| XI-5 | 실시간 스케줄링 개념 — 우선순위, RM/EDF, 우선순위 역전 | 제어 주기 보장 |
| XI-6 | 상태 기계 — FSM / 계층적 FSM / Behavior Tree | **작업 오더 상태머신**, 미션 실행기 |
| XI-7 | 이벤트 루프와 백프레셔 | 메시지 폭주로부터 시스템을 지키는 법 |
| XI-8 | 타이머 휠과 지연 큐 | 타임아웃, 재시도 |
| XI-9 | 공간 인덱스 — 그리드 해싱, KD 트리, 옥트리, R 트리 | **최근접 탐색, 충돌 검사, 포인트클라우드** |
| XI-10 | 그래프의 메모리 표현 — CSR, 인접 리스트 vs 행렬의 실제 비용 | 대규모 맵 |
| XI-11 | 순환 참조와 소유권 — 그래프 자료구조의 메모리 관리 | 스마트 포인터 사이클 |

### Part XII — 설계 패턴 ★ 우선순위 상

*GoF 카탈로그를 나열하지 않는다. **실제 시스템에서 그 패턴이 박혀 있는 지점**을 먼저 보여주고 이름을 나중에 붙인다.*

| # | 제목 | 대표 실사례 |
|---|---|---|
| XII-1 | 패턴을 왜 배우는가 — 이름 붙이기의 힘, 남용의 위험 | |
| XII-2 | **Adapter** | **이기종 장비 프로토콜 변환 계층.** 이게 없으면 기종이 늘 때마다 상위 로직을 고쳐야 하는 이유 |
| XII-3 | Facade / Bridge / Proxy | 서브시스템 단일 창구, 원격 객체 프록시, 지연 로딩 |
| XII-4 | Decorator / Composite | Behavior Tree 노드 합성, 레이어 중첩, 미들웨어 체인 |
| XII-5 | Factory Method / Abstract Factory / Builder | 드라이버·플러그인 생성, 설정 기반 객체 조립 |
| XII-6 | **Strategy** | **경로 플래너·컨트롤러 교체(플러그인), 압축·인코딩 교체** |
| XII-7 | **Observer / Publish-Subscribe** | **토픽·이벤트 버스·메시지 브로커의 정체**, 콜백 지옥과 그 해법 |
| XII-8 | **State** | **작업 오더 상태머신.** if-else 지옥 vs 상태 객체 |
| XII-9 | **Command** | **즉시 액션, 작업 큐, 실행 취소·재시도** |
| XII-10 | Template Method / Chain of Responsibility | 알고리즘 골격 고정, 에러 핸들러 체인, 미들웨어 |
| XII-11 | Mediator | **트래픽 매니저 — 개체끼리 직접 말하지 않고 중재자를 통한다** |
| XII-12 | Singleton과 그 함정 | 제어 소프트웨어에서 싱글톤이 재앙이 되는 경로, 테스트 불가능성 |
| XII-13 | 동시성 패턴 — Active Object, Reactor, Monitor, Half-Sync/Half-Async | XI과 연결 |
| XII-14 | 아키텍처 패턴 — 계층형(Sense-Plan-Act), 포트&어댑터, 플러그인 아키텍처, 블랙보드, 파이프-필터 | |
| XII-15 | 안티패턴 — God Object, 싱글톤 남용, 스마트 UI, 순환 의존, 조기 추상화 | |
| XII-16 | 패턴 선택 결정 트리 | Part I과 같은 형식 |

### Part XIII — 상태추정·인지 알고리즘

| # | 제목 |
|---|---|
| XIII-1 | 점유 격자 지도와 로그 오즈 갱신 |
| XIII-2 | 베이즈 필터의 뼈대 — 예측/보정 |
| XIII-3 | 칼만 필터와 EKF (수식 최소화, 직관 우선) |
| XIII-4 | 파티클 필터 |
| XIII-5 | ICP — 점군 정합 |
| XIII-6 | RANSAC — 이상치에 강건한 모델 추정 |
| XIII-7 | 포즈 그래프 최적화와 루프 클로저 |
| XIII-8 | 이미지 기본 연산 — 컨볼루션, 형태학, 엣지 |
| XIII-9 | 허프 변환과 특징점(ORB) 매칭 |

### Part XV — 시스템 규모의 자료구조 ★ 신설

*Part II의 자료구조는 전부 "메모리에 다 들어간다"를 가정한다. 그 가정이 깨지는 순간 무엇이 달라지는가. 코딩테스트에는 안 나오고 실무 시스템에는 전부 들어 있는 영역이다.*

| # | 제목 | 실사례 |
|---|---|---|
| XV-1 | **메모리를 벗어나는 순간** — 캐시 계층, 페이지, 블록 I/O. 왜 인메모리 자료구조가 디스크에서 무너지는가 | 이 Part 전체의 전제 |
| XV-2 | **B-Tree와 B+Tree** — 높이를 낮춰 디스크 읽기 횟수를 줄인다. 분할·병합, 클러스터드 인덱스 | **관계형 DB 인덱스, 파일시스템** |
| XV-3 | **LSM Tree와 SSTable** — 쓰기를 순차화한다. memtable, 컴팩션, 쓰기 증폭 vs 읽기 증폭 | **RocksDB, LevelDB, Cassandra, 시계열 DB** |
| XV-4 | **블룸 필터와 쿠쿠 필터** — 거짓양성을 허용하고 공간을 줄인다. 오차율 계산 | **LSM의 SSTable 스킵, 캐시 미스 방지, 크롤러 중복 제거** |
| XV-5 | **스킵 리스트** — 확률로 균형을 잡는다. 락프리 친화적인 이유 | **Redis Sorted Set, 동시성 인덱스** |
| XV-6 | **카디널리티·빈도 추정** — HyperLogLog, Count-Min Sketch. 정확도를 메모리와 교환한다 | **유니크 방문자 수, 핫키 검출** |
| XV-7 | **머클 트리와 해시 체인** — 부분만 비교해 차이를 찾는다 | **Git 오브젝트 그래프, 복제본 동기화, 무결성 검증** |
| XV-8 | **일관성 해싱** — 노드가 늘고 줄 때 재배치를 최소화한다. 가상 노드, 랑데부 해싱 | **샤딩, 분산 캐시, 로드 밸런서** |
| XV-9 | **캐시 교체 정책** — LRU/LFU/ARC/W-TinyLFU를 직접 구현하고 히트율을 비교한다 | **CPU 캐시, CDN, 애플리케이션 캐시** |
| XV-10 | **레이트 리미팅** — 토큰 버킷, 리키 버킷, 슬라이딩 윈도우 카운터·로그 | **API 게이트웨이, 백프레셔(XI-7과 연결)** |
| XV-11 | **복제와 합의 입문** — 왜 어려운가, Raft의 뼈대(리더 선출·로그 복제), 벡터 클럭, CRDT | **분산 KV 스토어, 멀티 마스터 동기화** |
| XV-12 | **메모리에 안 들어가는 데이터 처리** — 외부 정렬, 병합 전략, 스트리밍 집계, MapReduce의 자료구조적 정체 | **대용량 로그 처리, ETL** |

### Part XIV — 종합

| # | 제목 |
|---|---|
| XIV-1 | 실전 체크리스트 |
| XIV-2 | **기술 면접 CS 총정리** — 전 Part의 `::: interview` 집대성 |
| XIV-3 | 시스템 설계형 질문 대비 — 자료구조 선택으로 답하는 뼈대 |
| XIV-4 | 알고리즘 계보 한 장 지도 |

> 표시 순서는 Part XV → Part XIV다. Part XIV는 항상 마지막이다.

---

## 7. 리포 구조

```
Algo_MasterB/
  index.html
  build.py                      # 번들링 + ::: dual / classify / trace 파서 + --single
  .nojekyll                     # GitHub Pages
  assets/
    style.css
    markdown.js                 # 확장 문법 파서
    highlight.js                # Python + C++ 하이라이팅
    app.js                      # 라우팅·검색·진도·SRS·언어 토글
    bundle.js                   # 빌드 산출물, 커밋함
    widgets/
      grid-search.js  dp-table.js  heap-ops.js  hash-table.js
      union-find.js  binary-search-bounds.js  sorting-race.js
      tree-traversal.js  backtracking-tree.js  segment-tree.js
      dstar-replan.js  multi-agv-conflict.js  ringbuffer-concurrency.js
      state-machine.js  btree-ops.js  lsm-compaction.js  bloom-filter.js
      consistent-hash-ring.js  cache-policy.js  skiplist.js
      two-pointer.js  complexity-plot.js  kdtree-nn.js
  content/
    toc.json
    glossary.json
    toolbox/  classify/  ds-basic/  bruteforce/  graph/  shortest-path/
    greedy/  binsearch/  dp/  advanced/  math/
    realtime/  patterns/  perception/  scale/  wrapup/
  docs/
    STYLE.md                    # 집필 규범 전문
    LINK_LOG.md                 # 외부 링크 검증 로그
    PROBLEM_INDEX.md            # 유형 ↔ 대표문제 색인
    PROGRESS.md                 # 마일스톤 진행 기록
  tools/
    check_diagrams.py           # 아스키 다이어그램 정렬
    check_links.py              # 내부 id·위젯 타입 참조 무결성
    check_content.py            # §7-1
    export_flashcards.py        # Anki CSV 3종
    bench/                      # 본문 수치의 측정 스크립트 (§1-3)
  dist/
    anki/  01_classify.csv  02_complexity.csv  03_patterns.csv
  algobook.html                 # 단일 파일 빌드 산출물, 커밋함
  .github/workflows/ci.yml
  CLAUDE.md
  README.md
```

**빌드 타임 외부 도구 없음.** 표준 라이브러리 Python만으로 전부 돈다.

### 7-1. `tools/check_content.py`

`build.py` 끝에서 호출하되 실패해도 빌드는 막지 않는다(경고만). CI에서는 경고 개수를 출력한다.

1. **이중 언어 누락:** `::: dual`에 `python`과 `cpp`가 둘 다 있는가. 한쪽만 있으면 파일·줄 번호와 함께 경고.
2. **`::: trace` 누락:** §4-5 필수 목록 챕터에 있는가.
3. **`::: classify` 누락:** 알고리즘 챕터에 있는가. 없으면 플래시카드 덱에 구멍이 난다.
4. **복잡도 표기 누락:** `::: dual` 뒤 20줄 안에 `O(` 표기가 있는가(느슨한 검사).
5. **용어 사전 갱신:** 새 챕터 대비 `glossary.json` 추가 항목이 0개면 경고.
6. **밀도:** 본문이 6,000자 미만이면 경고(§4).
7. **`::: classify` 4항목:** 신호어/제약조건/혼동 주의/반례 함정이 다 있는가.

### 7-2. 커밋 컨벤션

Conventional Commits(`feat:`, `docs:`, `fix:`, `chore:`, `build:`). **커밋 메시지·PR 본문·코드 주석에 AI 어트리뷰션을 넣지 않는다.** §4-7이 그대로 적용된다. 위젯 JS 주석은 "무엇"이 아니라 **"왜 이렇게 구현했는가"**(예: 왜 `requestAnimationFrame`이 아니라 스텝 기반인가)를 남긴다.

### 7-3. 배포

- **마일스톤마다 별도 브랜치 + 드래프트 PR.** 브랜치명 `claude/m{N}-{요약}`. 머지 후 다음 마일스톤 브랜치를 새로 딴다.
- **GitHub Pages:** `main` / `/(root)`. `.nojekyll` 필수(`_`로 시작하는 경로가 무시되는 것을 막는다).
- **`algobook.html`을 매 마일스톤 커밋한다.** 폰에서 항상 최신본을 받을 수 있어야 한다.
- **CI(`.github/workflows/ci.yml`):** 매 푸시마다 `build.py` → `check_content.py` → `check_links.py` → `check_diagrams.py`. 빌드 실패와 깨진 내부 링크는 CI 실패, 콘텐츠 규범 경고는 로그로만.

---

## 8. UI/UX

- **좌측 목차 + 우측 인북 목차 + 본문** 3단. 검색(`/`, `Ctrl+K`), 테마 토글(다크/라이트), 글자 크기, 단축키(`[`/`]` 이전·다음 절, `Esc` 닫기).
- **언어 토글(전역).** 상단에 `Python | C++ | 둘 다`. 선택이 모든 `::: dual` 블록에 일괄 적용되고 localStorage에 저장. 기본값 `둘 다`.
- **SRS 복습 진입점.** 홈에 "오늘 복습할 N개".
- **완전 반응형**(A-13):
  - `≥ 1400px` — 3단. 위젯은 본문 폭을 넘어 렌더링 허용(`grid-search` 4분할은 넓을수록 좋다).
  - `1000~1400px` — 우측 인북 목차 접힘. 위젯 4분할 → 2×2.
  - `700~1000px` — 좌측 목차 오버레이 드로어. 위젯 2×2 유지.
  - `< 700px` — 단일 컬럼. 위젯은 세로 스택 + 가로 스크롤 컨테이너. 코드 블록은 자체 `overflow-x`.
  - **본문(`body`)은 어떤 폭에서도 가로 스크롤이 생기지 않는다.**
- **문제 색인 페이지.** `docs/PROBLEM_INDEX.md`를 앱 안에서 유형별로 필터 탐색.
- **용어 사전 페이지.** `glossary.json` 기반 가나다/알파벳 목록, 클릭 시 최초 정의 챕터로 이동.

---

## 9. 마일스톤

빅뱅 금지. 각 마일스톤 끝에 **실제로 빌드하고 열어서 동작 확인**(빈 화면·콘솔 에러 없음, 위젯이 인터랙션에 반응함) 전에는 다음으로 넘어가지 않는다. 확인 후 커밋 → 푸시 → 드래프트 PR → `docs/PROGRESS.md` 갱신 → 한 줄 보고.

| # | 내용 | 완료 기준 |
|---|---|---|
| **M0** | CLAUDE.md 확정 + README + `docs/STYLE.md` + 레포 뼈대 | 규범이 문서로 고정됨 |
| **M1** | 엔진 구축 + `::: dual`/`::: classify`/`::: trace`/`::: widget` 파서 + 언어 토글 + SRS + `toc.json` + 반응형 셸 + 더미 콘텐츠 1개 + CI | 파이프라인이 끝까지 동작 |
| **M2** | **★★★ 위젯 4종** — `grid-search`, `dp-table`, `heap-ops`, `hash-table` | 인터랙션이 실제로 반응 |
| **M3** | **Part 0 + Part I 완성** | 여기부터 학습 시작 가능. **가장 중요한 마일스톤** |
| **M3.5** | 단일 HTML + Anki 파이프라인 검증 | 폰에서 실제로 열림 |
| **M4** | Part II~IV | |
| **M5** | **Part V 전체** + `dstar-replan`, `multi-agv-conflict` | 이 책의 척추 완성 |
| **M6** | **Part XII (설계 패턴)** | 알고리즘 학습과 독립적으로 진행 가능하고 기술 면접 기대값이 높아 앞당긴다 |
| **M7** | Part VI~VIII | |
| **M8** | **Part XI (실시간·동시성)** | |
| **M9** | **Part XV (시스템 규모의 자료구조)** + `btree-ops`, `lsm-compaction`, `bloom-filter`, `consistent-hash-ring` | 시니어 실무 축 완성 |
| **M10** | Part IX~X | |
| **M11** | Part XIII~XIV | |

**순서 근거:** M3(유형 판별)를 최우선에 둔 것은 §1 A-06의 명제 때문이다. M6(설계 패턴)을 DP보다 앞에 둔 것은 그것이 알고리즘 학습과 독립적으로 진행 가능하고, 기술 면접에서의 기대값이 높기 때문이다. M9(Part XV)는 Part II·IX의 트리·해시 기반 위에서만 성립하므로 그 뒤에 둔다.

---

## 10. 작업 시작 전 자가 점검

0. **(최우선) §4-7을 위반하는 문장을 쓰려던 참은 아닌가?** 저자 1인칭 서사·이력·개인 사정·AI 어트리뷰션이 섞이려 하면 그 자리에서 멈추고 일반 명제로 고쳐 쓴다. 커밋 메시지와 PR 본문도 검사한다.
1. `docs/STYLE.md`를 읽었는가? 이미 있는 패턴을 재발명하려 들지 않는가?
2. 다른 레포·외부 문서에 설명을 떠넘기려 하지 않는가(§4-3, A-15)? 필요한 것은 여기서 끝낸다.
3. `::: dual`에 Python과 C++이 **둘 다** 있는가? 변수명·구조가 일치하는가? 언어 차이 표를 붙였는가? C++는 C++17에서 컴파일되는가?
4. `::: trace`를 넣었는가(§4-5)? 코드를 손추적보다 먼저 보여주지 않았는가(§4-2)?
5. `::: classify`의 4항목(신호어/제약조건/혼동주의/반례함정)이 다 있는가?
6. 복잡도에 **근거 한 줄**을 붙였는가(§4-4)?
7. 본문의 모든 수치를 §1-3 환경에서 **실제로 측정**했는가? 스크립트를 `tools/bench/`에 남겼는가?
8. 시간에 따라 변하는 개념을 텍스트로 때우려 하지 않는가(§4-1)?
9. 실무 연결이 억지는 아닌가(§4-6)? 억지면 쓰지 않고 보고한다.
10. 정답 코드 전문을 싣지 않았는가(§4-8)?
11. 새 용어를 `glossary.json`에, 외부 링크를 `LINK_LOG.md`에 같은 커밋으로 기록했는가?
12. §9 마일스톤 순서를 지키는가? 위젯을 전부 먼저 만들려 하지 않는가?
13. 좁은 폭에서 레이아웃이 깨지지 않는가(§8)?

---

## 11. 자율 실행 지침 — 언제 묻고 언제 진행하는가

**이 문서에 이미 답이 있는 것은 다시 묻지 않는다.**

기본 리듬은 **마일스톤 단위 자율 진행**이다. 한 마일스톤을 끝내고 검증·커밋·푸시·PR·보고까지 마친 뒤, 답변을 기다리지 않고 다음 마일스톤으로 넘어간다. §11-3에 해당하는 것을 실제로 발견했을 때만 멈춘다.

### 11-1. 이미 결정 끝남 — 그냥 실행

- §1 결정표, 아키텍처(§2), 비목표(§3), 집필 규범(§4), 위젯 목록·우선순위(§5), **Part/챕터 목차(§6)**, 리포 구조(§7), UI(§8), 마일스톤 순서(§9).
- **각 챕터의 절 단위 세분화, 예제 선택, 위젯 파라미터, 대표문제 선정, 아스키 다이어그램 형태** — 저술 판단이지 물어볼 결정이 아니다.
- **위젯 구현 기법**(Canvas vs SVG) — 위젯별로 더 간단한 쪽. 부족하면 나중에 고도화한다.
- **앱 표시 제목** 『Algorithmic』.

### 11-2. 진행하되 통보만 (질문 아님, 완료 후 한 줄 보고)

- 어떤 챕터에서 실무 연결점을 못 찾은 경우 → 억지로 만들지 말고 보고.
- 검증 가능한 외부 자료가 없는 경우 → 링크 없이 진행하고 보고.
- 목차의 챕터를 합치거나 쪼개는 것이 명백히 나은 경우 → 하고 보고. 단 **Part 단위 구조와 §1 결정표는 이 범위가 아니다**(→ 11-3).
- 측정해 보니 예상과 다른 수치가 나온 경우 → 실측값을 쓰고 보고.

### 11-3. 진짜로 물어야 하는 것 (드물어야 정상)

- **§1 결정표나 §2 아키텍처와 정면 충돌하는 기술적 사유를 실제로 발견한 경우.** 조용히 다르게 만들지 말고, 왜 안 되는지 설명하고 대안을 제시한 뒤 확인받는다.
- **§4-7 위반 여부가 애매한 문장을 발견한 경우.** 어떤 서술이 "대상 독자층 설명"인지 "저자 개인 서사"인지 스스로 판단하기 애매하면 조용히 정하지 말고 물어본다. 공개 커밋 이력은 되돌리기 어렵다 — 애매하면 보수적으로 쓰고, 그래도 애매하면 확인받는다.
- **금전·계정이 걸린 결정** (현재 스코프엔 없음).

**이 셋에 해당하지 않으면 묻지 말고 §9 마일스톤을 진행한다.**
