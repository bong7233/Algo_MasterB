# check_code.py 가 실행을 건너뛸 블록. 형식: 경로:줄 # 이유
#
# 여기 넣는 것은 "돌려 보면 실패하지만 본문에서는 옳은" 블록뿐이다.
# 대표적으로 앞 블록의 정의를 이어받는 조각, 그리고 일부러 실패하도록 만든 시연.
# 이유를 반드시 적는다. 건너뛰는 데 마찰이 있어야 아무거나 건너뛰지 않는다.

content/toolbox/0-12.md:106 # 스트레스 하네스가 버그를 잡고 종료 코드 1을 낸다. 그것이 이 블록의 시연이다
content/toolbox/0-9.md:179 # 시간 측정 블록. 두 언어의 실행 시간이 같을 수 없고, 측정 구간(N 범위)도 언어마다 다르게 잡는 것이 옳다
