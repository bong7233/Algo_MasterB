# 진행 기록

각 마일스톤 종료 시 한 항목씩 추가한다. 완료 기준은 `CLAUDE.md` §9.

기록할 것: 무엇을 만들었는가 / 무엇을 검증했는가 / §11-2에 해당하는 통보 사항(실무 연결이 약한 챕터,
링크를 못 붙인 곳, 목차를 조정한 곳, 예상과 다른 실측치).

---

## M0 — 규범 확정과 레포 뼈대

**상태:** 완료

**만든 것**

- `CLAUDE.md` — 최상위 컨텍스트. 결정표(A-01~A-15), 아키텍처, 집필 규범 요약, 전체 목차, 마일스톤.
- `docs/STYLE.md` — 집필 규범 전문.
- `README.md` — 공개 소개 문서.
- `docs/PROGRESS.md`, `docs/LINK_LOG.md` — 기록 문서.
- `.gitignore`, `.nojekyll`.

**확정된 방향 (착수 전 결정)**

| 사안 | 결정 |
|---|---|
| 프로젝트 성격 | **독립 프로젝트.** 외부 레포에 지식을 위임하지 않는다. 필요한 것은 전부 이 안에서 완결 |
| 폰 복습 | **단일 HTML**(`algobook.html`) + Anki CSV. EPUB은 위젯이 죽어서 비채택 |
| 학습 관리 | SM-2 SRS 신규 구현. 챕터가 아니라 `::: classify` / `::: quiz` 항목 단위로 스케줄링 |
| 화면 대응 | **완전 반응형.** 폰에서도 본문·목차·검색·위젯이 동작 |
| 중심축 | 코딩테스트와 실무를 동등하게. Part I(유형 판별)은 그대로 1순위 |
| 언어 기초 | Part 0을 자립적 완결 레퍼런스로 강화(11 → 13챕터) |
| 툴체인 | CPython 3.13 / g++ 13 `-std=c++17 -O2` / Ubuntu 24.04 |
| 목차 확장 | **Part XV「시스템 규모의 자료구조」12챕터 신설** — B-Tree, LSM, 블룸 필터, 스킵 리스트, HyperLogLog, 머클 트리, 일관성 해싱, 캐시 정책, 레이트 리미팅, 합의, 외부 정렬 |
| 배포 | 마일스톤마다 브랜치 + 드래프트 PR / GitHub Pages / `algobook.html` 매 마일스톤 커밋 / CI 검증 |

**통보 사항**

- Part XV 신설로 위젯이 6종 추가됐다(`btree-ops`, `lsm-compaction`, `bloom-filter`, `consistent-hash-ring`, `cache-policy`, `skiplist`). M9에서 구현한다.
- Part X에 X-7「부동소수점의 함정」을 추가했다. 기하와 실수 이분 탐색 양쪽에서 반복해서 필요한데 어느 챕터에도 자리가 없었다(§11-2 목차 조정).
- 컨테이너 기본 `python3` 는 3.11이다. 벤치마크는 명시적으로 `python3.13` 으로 실행한다.

---

## M1 — 엔진 구축

**상태:** 완료

**만든 것**

- `index.html` — 상단바, 3단 레이아웃, 검색·복습 모달, 오버레이 드로어.
- `assets/markdown.js` — `::: dual` / `::: classify` / `::: trace` / `::: widget` 을 포함한 확장 문법 렌더러.
- `assets/highlight.js` — Python·C++ 구문 강조기.
- `assets/style.css` — 테마 2종, 반응형 4단계, 상자 14종.
- `assets/app.js` — 라우팅·목차·검색·인북 목차·진도·SM-2 SRS·언어 토글·위젯 마운트.
- `build.py` — 번들링, 자산 버전 도장, `--watch` / `--serve` / `--single`.
- `tools/` — 콘텐츠 규범·내부 링크·다이어그램 정렬 검사기, Anki 내보내기.
- `content/toc.json` — 16개 Part 147개 절. `content/glossary.json` — 용어 31개.
- `content/toolbox/0-11.md` — 손추적 방법론. 데모이자 실제 챕터.
- `.github/workflows/ci.yml`.

**검증한 것**

| 대상 | 방법 | 결과 |
|---|---|---|
| 구문 강조기 | 줄별 span 균형, 무손실 왕복, 꺾쇠 이스케이프, 비정상 입력 | 7/7 통과 |
| 마크다운 렌더러 | 계약 전 항목 + 상자 중첩 + XSS + 슬러그 일치 | 24/24 통과 |
| 통합 | 실제 브라우저(Chromium)에서 렌더·상호작용·반응형 | 36/36 통과 |
| 가로 스크롤 | 1500·1300·900·700·420·360px | 전 폭에서 없음 |
| 단일 파일 | 폰 폭에서 열기 | 외부 요청 0, 콘솔 에러 0 |

**통보 사항**

- 계약서에 `.topbar-actions` 래퍼를 적지 않아 아이콘 버튼이 세로로 쌓여 상단바를 넘어가는 결함이 있었다. 브라우저 검증에서 잡아 수정했다. `overflow-x: clip` 때문에 눈으로는 보이지 않던 결함이다.
- 드로어가 열린 동안 상단바는 오버레이 아래에 놓인다. 스크림을 눌러 닫는 것이 표준 동작이므로 그대로 둔다.
- 위젯이 없는 `::: widget` 은 자리표시자로 렌더된다. 콘텐츠가 위젯보다 먼저 쓰이므로 정상적인 중간 상태다.

---

## M2 — 간판 위젯 4종

**상태:** 완료

**만든 것**

- `assets/widgets/_runtime.js` — 위젯 공용 런타임. 껍데기 DOM, 스텝 재생기, 고해상도 캔버스, 폭 변화 대응, 테마 색 읽기.
- `assets/widgets/grid-search.js` — 같은 격자에서 BFS/DFS/다익스트라/A\* 4분할 동시 재생. 방문 순서를 색 농도로. 벽 드래그 편집.
- `assets/widgets/dp-table.js` — 배낭/LCS/LIS/구간DP/격자 5종. 점화식이 읽는 칸으로 향하는 화살표와 역추적.
- `assets/widgets/heap-ops.js` — sift-up/sift-down. 트리와 배열을 동시에, 같은 노드를 양쪽에서 강조.
- `assets/widgets/hash-table.js` — 해시→버킷→충돌→탐사→리해싱. 체이닝·선형·이차·이중 해싱.
- `assets/style.css` 20절 — 위젯 런타임 크롬.

**검증한 것**

| 항목 | 결과 |
|---|---|
| 13가지 opts 조합 렌더 | 통과 |
| 되감기 결정성(픽셀 해시) | 4종 통과 |
| 상태 줄이 스텝마다 바뀜 | 4종 통과 |
| 비정상 opts 7종 내성 | 4종 통과 |
| 1200·900·700·400px 렌더 + 가로 스크롤 없음 | 16조합 통과 |
| 테마 전환 시 다시 그림 | 4종 통과 |
| 콘솔 에러 | 0건 |

스크린샷을 직접 읽어 산출물을 눈으로 확인했다. 배낭 `dp[3][6] = max(dp[2][6]=7, dp[2][2]+5=8) = 8`,
해시 `318 % 16 = 14`, 힙의 트리·배열 일치를 손으로 검산했다.

**통보 사항**

- `dp-table` 이 큰 표를 조용히 잘라내고 있었다(용량 30→14, 물건 8→6). 자르는 것 자체는 가독성을 위해 맞지만 잘렸다는 사실이 감춰졌다. 제목에 "표가 읽히도록 줄임: 용량 30→14, 물건 8→6" 을 밝히도록 고쳤다.
- `grid-search` 는 700px 미만에서 4분할을 포기하고 한 번에 하나씩 보인다. 그 폭에서는 비교를 잃더라도 가독성이 우선이다.
- 기본 격자에서 A\* 확장 124칸 대 다익스트라 134칸으로 차이가 크지 않다. 벽이 우회를 강제하기 때문이다. V-5 를 쓸 때 열린 격자 프리셋을 함께 보이는 편이 낫다.

---

## M3 — Part 0 + Part I 완성

**상태:** 완료. **여기부터 학습을 시작할 수 있다.**

**만든 것**

Part 0 도구상자 13챕터, Part I 유형 판별 7챕터. 본문 181,618자(약 130쪽).
`complexity-plot` 위젯, `tools/check_code.py`, `tools/bench/` 측정 스크립트 40여 개.

**검증한 것**

| 대상 | 결과 |
|---|---|
| `build.py` | 에러 0 |
| `check_links.py` | 내부 링크 이상 없음, 경고 0 |
| `check_diagrams.py` | 이상 없음 |
| `check_code.py` | `::: dual` 25개 전부 실행, 실패 0, 선언 없는 불일치 0 |
| `check_content.py` | 경고 1건 (i-5 분량 초과 — 아래 참조) |
| 단일 파일 | `algobook.html` 0.9MB |
| Anki | 3덱 (유형판별 13 / 복잡도 25 / 용어 73장) |

**실측이 통설을 뒤집은 것 세 가지** — 측정 규범이 없었으면 틀린 책이 됐을 지점이다.

1. **`input()`이 느린 이유는 개행 제거가 아니다.** `rstrip` 비용은 전체 차이의 5%뿐이고 나머지는 `input()` 자신의 호출 비용이다.
2. **`s += t`는 루프에서 항상 O(n²)이 아니다.** CPython은 참조 수가 1이면 제자리 확장한다. 참조 1개는 선형(`join` 대비 1.7배), 참조 2개는 제곱(1,799배). 조건이 코드에 보이지 않는다는 것이 `join`을 권하는 근거다.
3. **`x + 1 > x`는 `-O0`에서도 true로 접힌다.** "최적화를 끄면 안전하다"가 사실이 아니다.

**§11-2 통보 사항**

- **i-5 오분류 사례집이 13,326자로 상한 12,000자를 넘는다.** 사례 여섯 개가 전부 손으로 검산되는 반례라 덜어낼 곳이 없다. 사례집은 한 사례씩 끊어 읽는 구조이므로 "길면 안 읽는다"는 상한의 취지가 선형 논증만큼 강하게 적용되지 않는다고 판단해 그대로 둔다. 검사기 경고는 남겨 둬 다음 편집자가 다시 판단할 수 있게 했다.
- `check_content.py` 에 **분량 상한 검사를 추가**했다. 규범은 6,000~12,000자인데 하한만 검사하고 있었다.
- **조각 코드 표기 규약**(`title="… (조각) …"`)을 세웠다. 예외 파일에 넣어 조용히 넘기면 독자는 모르는 채로 남는다. 제목 표기 하나가 독자와 검사기에게 동시에 말한다.
- 백준 문제는 `acmicpc.net` 이 프록시에서 403이라 검색 결과에 노출되는 사이트 자신의 제목 태그(`NNNN번: 제목`)로 번호·제목을 대조했다. **티어를 확인 못 한 1010 다리 놓기는 뺐다.**
- Part 0 의 0-2·0-3·0-4·0-13 은 실무 연결이 얕다. 언어 레퍼런스라 "이 자료구조가 어느 시스템에 박혀 있는가"는 해당 자료구조 챕터의 몫이다. 억지로 넣지 않고 각 항목에 "어디서 쓰이는가"를 챕터 링크로 달았다.
- I-7 의 시간 배분 수치는 **측정이 아니라 산술**임을 본문에서 명시했다. 근거 없는 수치를 실측인 양 적지 않았다.
